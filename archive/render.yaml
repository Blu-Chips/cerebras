services:
  - type: web
    name: mpesa-analyzer
    env: node
    buildCommand: cd frontend && npm install && npm run build   # ✅ Updated
    startCommand: node server.js
    staticPublishPath: frontend/build
    envVars:
      - key: CEREBRAS_API_KEY
        sync: false